<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/transparent" /> <!-- Cor de fundo transparente -->
    <corners android:radius="16dp" /> <!-- Bordas arredondadas -->
    <stroke
        android:width="2dp"
        android:color="@color/icon_medium" />
</shape>
